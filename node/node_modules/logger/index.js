module.exports = {
	log: function () {
		if (global.env.level == 'prod') {
			global.rollbar.log(...arguments)
		} else {
			console.log(...arguments)
		}
	},
	error: function () {
		if (global.env.level == 'prod') {
			global.rollbar.error(...arguments)
		} else {
			console.error(...arguments)
		}
	}
}